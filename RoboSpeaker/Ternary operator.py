# Ternary operator
is_friend = True
can_message = "allowed to message" if is_friend else "Not allowed to message"

print(can_message)

is_old = False

drive = "Can drive a car" if is_old else "Can not drive the car"

print(drive)